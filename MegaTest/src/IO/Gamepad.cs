using MegaLib.IO;
using NUnit.Framework;

namespace MegaTest.IO;

public class GamepadTest
{
  [Test]
  public void TestBasic()
  {
    /*var state = new Gamepad.XInputState();

    while (true)
    {
      var result = Gamepad.XInputGetState(0, ref state); // 0 - это индекс контроллера

      if (result == Gamepad.ERROR_SUCCESS)
      {
        Console.WriteLine("Геймпад подключен.");
        Console.WriteLine("Buttons: " + state.Gamepad.Buttons);
        Console.WriteLine("Left Trigger: " + state.Gamepad.LeftTrigger);
        Console.WriteLine("Right Trigger: " + state.Gamepad.RightTrigger);
        Console.WriteLine("ThumbLX: " + state.Gamepad.ThumbLX);
        Console.WriteLine("ThumbLY: " + state.Gamepad.ThumbLY);
        Console.WriteLine("ThumbRX: " + state.Gamepad.ThumbRX);
        Console.WriteLine("ThumbRY: " + state.Gamepad.ThumbRY);
      }
      else if (result == Gamepad.ERROR_DEVICE_NOT_CONNECTED)
      {
        Console.WriteLine("Геймпад не подключен.");
      }

      System.Threading.Thread.Sleep(100); // Пауза для уменьшения нагрузки на процессор
    }*/
  }
}